/*
 * Public API Surface of @swipergy/swipyui
 *
 * Components live in their own entry points, e.g.
 * `@swipergy/swipyui/button`. The primary entry point exposes
 * the version and re-exports the core utilities.
 */

export const VERSION = '0.1.0';

export * from '@swipergy/swipyui/core';
